{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "cd backend && python manage.py migrate --noinput ; daphne -b 0.0.0.0 -p ${PORT:-8000} backend.asgi:application"
  }
}
